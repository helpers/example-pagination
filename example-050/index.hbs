---
title: example-050
layout: <%= example %>/layout.hbs
---

{{#each pages}}
  <!-- page -->
  {{#is this.basename "index"}}
  {{else}}
    {{#include}}
      {{{page}}}
    {{/include}}
  {{/is}}
{{/each}}